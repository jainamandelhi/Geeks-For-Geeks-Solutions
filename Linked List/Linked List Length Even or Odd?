int func(Node *head)
{
    if(!head)
        return 0;
    return 1+func(head->next);
}
int isLengthEvenOrOdd(struct Node* head)
{
    int ans=func(head);
    if(ans%2)
        return 1;
    else
        return 0;
     //Code here
}
